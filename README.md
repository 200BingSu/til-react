# useMemo

- `React를 최적화`하였다: 리랜더링 횟수를 줄인다.

## 레이아웃 최적화를 했는가?

- `Shift Layout` 적용했는가
- css로 직접 만들거나 npm을 이용하여 해결

## 리랜더링 최적화를 했는가?

- `useMemo()`: 변수 관리 했어요?
- `useCallback()`: 함수 관리 했어요?
- `React.memo()`: 컴포넌트 관리 했어요?

## Lazy Loading 했는가?

## 이미지 최적화 했는가?

- web...

## SEO 최적화 했는가?

## GA4 적용 했어요?

(어렵다..!)
